# V57.5 Resource Detection Fix

Branch created after V57.4 scan test.

Goal: improve resource-tile detection so the analyzer reports individual visible RSS tiles (type + level + occupancy) rather than only aggregate/validated RSS counts.

Testing target: map scanning only. Auto-gather remains disabled until detection is reliable.
